track:
    built when anything is generated with api calls
        booleans all the chart windows
        booleans all the additional_info p/e windows

booleans for error popup windows


NEED TO GO OVER API FOR P/E window
NEED TO GO OVER API FOR CHART GENERATION


BUTTON PLACEMENT STRATEGIES


PRECISION SCRIPT IN CASE STD::TO_STRING() CAUSES ISSUES
#include <iomanip>
#include <sstream>

double price_change = stock.get_price_change();

// Manually round to 2 decimal places if needed
std::stringstream ss;
ss << std::fixed << std::setprecision(2) << price_change;
std::string price_change_str = ss.str();

// Now, price_change_str is properly formatted to 2 decimal places
const char* price_change_cstr = price_change_str.c_str();

ImGui::Text("%s", price_change_cstr);


APIKEY
d9e656a793mshfb457422b030792p1728dcjsn066cc8928220




ACCOUT FOR THIS
terminate called after throwing an instance of 'Json::LogicError'
  what():  in Json::Value::resolveReference(key, end): requires objectValue
Aborted (core dumped)
